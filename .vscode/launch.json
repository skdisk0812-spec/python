{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python 실행 및 디버그",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}",
            "justMyCode": true
        }
    ]
}
